services:
  yayo-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yayo-frontend
    ports:
      - "5174:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
